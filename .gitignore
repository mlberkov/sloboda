__pycache__/
*.pyc
.venv/
.pytest_cache/
# Отчёты прогонов — рабочая выдача, а не след в истории: на первом `git add -A`
# неотслеживаемые reports/run-*.md|json уехали бы в коммит пачкой. Уже
# отслеживаемые отчёты этим правилом с учёта не снимаются: вопрос «хранить ли
# отчёты как след и где» (кандидат — archive/) решается не этим пакетом.
reports/
