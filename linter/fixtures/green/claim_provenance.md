# GREEN · claim_provenance

Owner-блок закрытия пакета.

## Стоп-условия пакета

- Впереди main на 6 коммитов — выдача `git rev-list --count main..HEAD`
- В diff 4 файла — выдача `git diff --stat`, строка «4 files changed»
- Число ревизий сервиса — ожидание, в этом ходе не проверено

## Основание выбора дефолта

Темп правок канона — примерно 5 в неделю; это рамка оркестратора, не проверено.

За один разбор внесено 5 из 8 кандидатов — выдача `git show --stat HEAD`.

## Адрес пункта

Пункт «Действия владельца — отдельным блоком, а не прозой» стоит в §7 контракта —
прочитано в этом ходе, `sed -n '113,115p' theygrow-delivery-contract.md`.

Тот же адрес назван в логе вольта: 00-system/log.md:2235 — цитата выдачи
`grep -n "отдельным блоком" 00-system/log.md`.

Формулировка занимает строку 113 файла контракта — из выдачи той же команды.

**Конец хода:** нужно слово владельца — сверить строки выше со своей консолью и назвать расхождение.
