# GREEN · smoke_line

Тот же предмет, собранный по проверке пункта: у каждого блока, выданного
владельцу, рядом названо ожидание вывода — либо цитатой фактического вывода на
предмете, либо пометкой «не исполнялась — первый прогон у владельца».

## Ваши действия

1. Прогнать детектор публичной политики и прислать вывод целиком:

```bash
test -s public/policy.html || printf 'ВХОД ПУСТ: public/policy.html\n'
grep -c "Версия 1.3" public/policy.html
```

Ожидаемо: `1` — фактический вывод на предмете, прогон оркестратора 2026-08-31.

2. Прогнать ту же меру в owner-окружении (авторизованный `gcloud`) — не исполнялась, первый прогон у владельца:

```bash
gcloud run services list --region europe-west1
```

Ожидание: ровно одна строка с именем службы и её регионом.

**Конец хода:** нужно слово владельца — подтвердить, что вывод у него совпал с процитированным выше.
