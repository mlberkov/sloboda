# RED · download_dir

Артефакты прогона нужны владельцу для разбора; блок выгружает их прямо в дерево.

## Ваши действия

1. Выгрузить артефакты и прислать перечень файлов:

```bash
gh run download 18422331 --repo theygrow/app --event workflow_dispatch --commit HEAD
gh release download v0.1.221 --repo theygrow/app -D artifacts/release
```

Ожидаемо: два каталога с файлами — фактический вывод на предмете, измерено оркестратором 2026-08-31.

**Конец хода:** нужно слово владельца — прислать перечень выгруженного.
