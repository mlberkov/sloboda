# RED · whitespace_diff

Правка политики пришла от исполнителя; блок сверки считает строки, но фильтра
пробелов не несёт.

## Ваши действия

1. Сверить правку и прислать вывод целиком:

```bash
git diff --stat HEAD~1 -- public/policy.html
git diff --numstat HEAD~1 -- public/
```

Ожидаемо: две строки счётчиков — фактический вывод на предмете, измерено оркестратором 2026-08-31.

**Конец хода:** нужно слово владельца — назвать, что он видит в счётчиках.
