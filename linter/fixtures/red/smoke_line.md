# RED · smoke_line

Опора: контракт §11, пункт R-SMOKE-004 (реестр `rules/registry.yaml`); реестр
правок канона §B, рецидив 2026-08-28, счёт 1 — шаблон грепа построен на
невиданной разметке, пустая выдача едва не прочитана как факт. Оба случая ниже
выданы владельцу блоком: первый несёт ожидание из головы, второй — не несёт
ожидания вовсе.

## Ваши действия

1. Прогнать детектор публичной политики и прислать вывод целиком:

```bash
grep -c "Версия 1.3" public/policy.html
```

Ожидаемый вывод — 1.

2. Прогнать линтер полосы и прислать вердикт:

```bash
cd ~/sloboda && .venv/bin/python run.py --full; echo "код выхода: $?"
```

**Конец хода:** нужно слово владельца — подтвердить, что обе строки выше он готов прогнать у себя.
