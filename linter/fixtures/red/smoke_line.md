# RED · smoke_line

Опора: контракт §11, пункт R-SMOKE-004 (реестр `rules/registry.yaml`); реестр
правок канона §B, рецидив 2026-08-28, счёт 1 — шаблон грепа построен на
невиданной разметке, пустая выдача едва не прочитана как факт. До сценария S-06
норму не держал ни один активный сценарий: реестр числил её мёртвой.

## Случай 1 — вывод не цитируется

**Строка смоука** для owner-проверки публичной политики:

```bash
grep -c "Версия 1.3" public/policy.html
```

Ожидаемый вывод — 1.

## Случай 2 — пометка без названного ожидания

**Строка смоука** для owner-окружения (авторизованный `gcloud`):
не исполнялась — первый прогон у владельца.

**Конец хода:** нужно слово владельца — подтвердить, что обе строки выше он готов прогнать у себя.
