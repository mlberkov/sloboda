# RED · owner_action_block

Линтер дособран: два чекера получили формы, реестр сведён с вольтом. Ниже — что
нужно от владельца, чтобы закрыть ход.

## Ваши действия

1. Прогнать линтер на фикстурах и прислать вывод целиком:

```bash
cd ~/sloboda && .venv/bin/python run.py --full; echo "код выхода: $?"
```

2. После зелёного прогона закоммитить полосу и запушить — сообщение коммита
возьмите из заголовка разбора выше.

**Конец хода:** жду вывод прогона и хэш коммита.
