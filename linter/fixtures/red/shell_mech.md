# RED · shell_mech

Handoff for shell — блок, нарушающий все четыре вопроса механики.

````bash
set -euo pipefail
PROJECT=<PROJECT_ID>
REGION=<REGION-NAME>
cat > /tmp/report.md <<"MD"
## Отчёт
```
вложенная ограда внутри блока
```
MD
echo "шаг 1 из длинного блока"
echo "шаг 2 из длинного блока"
echo "шаг 3 из длинного блока"
echo "шаг 4 из длинного блока"
echo "шаг 5 из длинного блока"
echo "шаг 6 из длинного блока"
echo "шаг 7 из длинного блока"
echo "шаг 8 из длинного блока"
echo "шаг 9 из длинного блока"
echo "шаг 10 из длинного блока"
echo "шаг 11 из длинного блока"
echo "шаг 12 из длинного блока"
echo "шаг 13 из длинного блока"
echo "шаг 14 из длинного блока"
echo "шаг 15 из длинного блока"
echo "шаг 16 из длинного блока"
echo "шаг 17 из длинного блока"
echo "шаг 18 из длинного блока"
exit 1
````

Handoff for shell — блок коммита вольта, написанный в Windows-синтаксисе и выданный
на исполнение в bash/WSL (инцидент 2026-08-31).

```bash
cd D:\Obsidian\TheyGrow
git add -A
git commit -m "canon: правки дня"
git push
```
