# RED · grep_vacuum

Строка смоука для owner-проверки публичной политики.

```bash
grep -n "Версия 1.3" public/policy.html
rg "нативный экран" src/screens.tsx
```

Нулевая выдача здесь читается как «текста нет», хотя ровно так же выглядят
несовпавший шаблон и пустой файл.
